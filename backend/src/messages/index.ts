import { getMessageWithCtx } from './utils';

import loggerMessages from './messages.logger';

// Determine all messages
const messages = {
    logger: loggerMessages,
};

export { getMessageWithCtx };
export default messages;
